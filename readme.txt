==Usage:==
You should install Apache maven environment.
Build on Linux:
In the trunk root directory, run command:

    $ mvn site

to generate book.html with css and images resources directory.

or you can run:

    $ mvn -Donepage site

to generated onepage html.
the generated  html file is in target/docbkx

Andy Zhou
2013-1-7
